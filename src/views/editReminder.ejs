<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa Lời Nhắc</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <link rel="stylesheet" href="../CSS/reminders.css">
</head>

<body>
    <div class="container-box">
        <div class="top-bar">
            <button onclick="history.back()"><i class="fa-solid fa-arrow-left"></i></button>
            <span class="title">Lời nhắc nhở</span>
            <button><i class="fa-solid fa-gear"></i></button>
        </div>

        <div class="content">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h4>Sửa lời nhắc</h4>
                <a href="/view-reminders" class="cancel">Hủy</a>
            </div>

            <form action="/update-reminder" method="POST">
                <div class="mb-3">
                    <input type="text" id="id" class="form-control" name="id" value="<%= reminderEdit.id %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Tên khoản thanh toán</label>
                    <input type="text" id="name" class="form-control" name="reminderName"
                        value="<%= reminderEdit.payment_account_name %>">
                </div>

                <div class="mb-3">
                    <label class="form-label">Số tiền</label>
                    <input type="text" id="amount" class="form-control" name="amount"
                        value=" <%= reminderEdit.amount %>">
                </div>

                <div class=" mb-3">
                    <label class="form-label">Ngày đến hạn</label>
                    <input type="date" id="dueDate" class="form-control" name="dueDate"
                        value="<%= reminderEdit.due_date ? new Date(reminderEdit.due_date).toISOString().split('T')[0] : '' %>">

                </div>

                <div class=" mb-3">
                    <label class="form-label">Tần suất</label>
                    <select id="frequency" class="form-select" name="frequency" value="<%= reminderEdit.frequency %>">
                        <option value="Một lần">Một lần</option>
                        <option value="Hàng ngày">Hàng ngày</option>
                        <option value="Hàng tuần">Hàng tuần</option>
                        <option value="Hàng tháng">Hàng tháng</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label">Phương thức thanh toán</label>
                    <select id="paymentMethod" class="form-select" name="reminderMethod"
                        value="<%= reminderEdit.payment_method %>">
                        <option value="Ví điện tử">Ví điện tử</option>
                        <option value="Thẻ ngân hàng">Thẻ ngân hàng</option>
                        <option value="Tiền mặt">Tiền mặt</option>
                    </select>
                </div>

                <div class="mb-3">
                    <label class="form-label">Thông báo</label>
                    <select id="notification" class="form-select" name="reminderNotification"
                        value="<%= reminderEdit.notification_time %>">
                        <option value="1 ngày trước">1 ngày trước</option>
                        <option value="3 ngày trước">3 ngày trước</option>
                        <option value="1 tuần trước">1 tuần trước</option>
                    </select>
                </div>

                <button class="btn btn-save" onclick="saveReminder()" type="submit">Lưu</button>
            </form>
        </div>
    </div>
    <script src="/js/reminderScript.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>